secretsManagerTransit:
  host: host.minikube.internal

server:
  ha:
    replicas: 1

# server:
#   dev:
#     enabled: true
# injector:
#   affinity: null # Allow multiple pods per node (since minikube only has one node)
